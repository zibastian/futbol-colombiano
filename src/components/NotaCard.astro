---
interface Props {
  slug: string;
  title: string;
  description: string;
  tipo: string;
  cover?: string;
  coverAlt?: string;
}
const { slug, title, description, tipo, cover, coverAlt } = Astro.props;
// Ninguna tarjeta queda sin imagen: si la nota no trae portada, se usa la genérica.
const imagen = cover || '/portada-default.svg';
const etiquetas: Record<string, string> = {
  cronica: 'Crónica',
  fichaje: 'Fichajes',
  previa: 'Previa',
  agenda: 'Agenda',
  opinion: 'Opinión',
  reporte: 'Reporte',
  nota: 'Noticias'
};
---

<article class="nota-card">
  <a href={`/noticias/${slug}`}><img src={imagen} alt={coverAlt || title} loading="lazy" /></a>
  <div class="cuerpo">
    <span class="kicker">{etiquetas[tipo] || 'Noticias'}</span>
    <h3><a href={`/noticias/${slug}`}>{title}</a></h3>
    <p>{description}</p>
  </div>
</article>
